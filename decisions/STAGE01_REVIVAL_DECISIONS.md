# Stage 1 Revival Decisions

Date: 2026-09-10

Branch: `revival/stage00-coalition-repositioning`

Workflow: `ryotamatsuki/research-paper-workflow` v2.1 at `f48984013898696f010f0437a8cfed6b5b54bdc2`.

## Decision 1 — audited source authority

For the revival track, the audited PR #65 source representation is:

- `model/REVIVAL_STAGE1_AUDITED_REPRESENTATION_2026-09-10.md`;
- `verification/stage01_revival_source_audit.py`;
- `reviews/STAGE_01_REVIVAL_SOURCE_MATHEMATICAL_AUDIT_2026-09-10.md`.

Historical manuscripts, Salop formulas, old freezes, and later main-branch affine results are comparison evidence only and do not override this revival Stage-1 representation.

## Decision 2 — fixed-depth PR #65 result

The PR #65 fixed-depth member-welfare reversal is retained as a **valid computational source witness**, not a theorem:

- fixed positions: `W_member(SU)-W(IS) approximately -2.086e-4`;
- endogenous positions at the same fixed depths: approximately `+2.664e-4`.

The bilateral member firms' outward repositioning is also reproduced.

## Decision 3 — structural IS policy diagnosis

On the symmetric-anchor IS continuation,

`c(s)=c0+lambda/[4(t_bar-s)]-v`,

and

`dW_IS/dc = -a^2(2b^2+bc+c^2)/[4b^2(b+2c)^2] < 0`,

while `dc/ds>0`.

Therefore `s_I*=0` is **structural within the PR #65 source mapping**, not a canonical-parameter accident. The committed verifier finds the anchors globally deviation-proof at eleven policy nodes over `[0,s_bar]`.

This result is conditional on the symmetric-anchor continuation; no unrestricted all-parameter theorem is claimed.

## Decision 4 — economic interpretation of the blocker

The source architecture separates:

- discrete compatibility/network membership through `G(rho)`; and
- continuous standards depth through `Tau(rho,s)`.

Depth does not increase the network-benefit term. Under IS it therefore mainly increases competitive substitutability without adding a separate interoperability benefit. This is mathematically coherent but economically restrictive and is classified:

`CORRECT BUT ECONOMICALLY AD HOC`.

No repair is authorized at Stage 1.

## Decision 5 — endogenous-depth diagnostic

The canonical full-policy diagnostic remains negative for an SU member relative to IS:

`W_member(SU)-W_i(IS) approximately -2.8265e-4`.

The old local `3 x 3` `(v,gamma)` box also remains negative under the audited FULL candidate diagnostic.

This is repository-reproducible diagnostic evidence, not yet a continuous-domain global policy-equilibrium theorem.

## Decision 6 — source conflicts

The stale manuscript's statement that singleton blocs cannot choose positive depth conflicts with the later Stage-3 policy map and PR #65 implementation. For the revival source audit, the later policy map controls.

The complete post-affine blocking/consent correspondence across every ordered partition transition is not fully explicit in the inherited source set. The IS-versus-SU strict-blocking comparison is clear, but the full stable-partition operator must be frozen before any later Stage-4 coalition-stability theorem.

## Decision 7 — national-welfare convention

`W_i=CS/3+Pi_i` may be used as an inherited source convention only if the equal-country consumer aggregation is stated explicitly. The affine representative-consumer system by itself does not derive the one-third national split.

## Decision 8 — Stage-1 verdict

**GO TO NOVELTY GATE.**

The source is sufficiently reconstructed to support a serious Stage-2 whole-game novelty audit. No new model primitive is authorized.

## Decision 9 — Stage-2 mandatory comparison set

Stage 2 must include:

- Woeckener (1999);
- standards-coalition / network-competition literature;
- compatibility-policy and harmonization-depth literature;
- endogenous product differentiation / alliance literature;
- `private-compatibility-standards-coalitions` as an own nested benchmark;
- `standardization-scope-direction-innovation` as own internal prior art on continuous standardization policy inducing a downstream strategic firm response.

The revival may proceed to Stage 3 only if a whole-game or theorem-level distinction survives.
