# Repeat Stage 4A Decision Record — Revival C1

Date: 2026-09-15

## Decision

**GO — MATHEMATICAL ADVERSARIAL CERTIFICATION PASS.**

Route to **Stage 6 — Novelty Re-Kill**.

## Binding scope

The repaired theorem scope is the higher-repositioning-cost continuation class centered on `(v,gamma)=(.11,.10)`.

The original low-`gamma` selection-free claim remains false. The crossing equilibrium at `gamma=.03` is permanent regression evidence and may not be suppressed by any implicit selection convention.

No equilibrium-selection rule, no-crossing constraint, additional primitive, or model change was introduced.

## Certified repaired objects

1. C1 demand/Bertrand continuation survives independent attack on the stated domain.
2. At the repaired headline histories, independent full-system multi-start + whole-circle deviation search finds one retained pure location Nash equilibrium for IS, SU, and SW.
3. The SU repaired continuation survives a 15-point high-gamma full-depth stress box and 81 material policy-history alternative-equilibrium attacks without a second pure Nash being found.
4. The repaired SU policy candidate remains `(.25,.25)` under downstream-re-solved unilateral depth attacks.
5. At `(v,gamma)=(.11,.10)`, FULL SU members strictly prefer SU to IS and SW; the strict-blocking stable set is the three bilateral SUs.
6. B-FIX retains IS at the same primitives because `v_FIX=1/15` and `.11>1/15`.
7. At `gamma=.10`, the independently reconstructed thresholds are `v_EXO-HIST≈.0993400329` and `v_FULL≈.1196400688`.

## Prohibited overstatement

The project may not claim:

- global uniqueness of the SU location game for all `gamma`;
- a universal analytic bifurcation threshold in `gamma`;
- that policy endogeneity is generically necessary based on B-EXO-HIST;
- that the low-`gamma` stable-set result is selection-free.

## Formal verification

Applicability remains **YES**. Implementation remains pending for Stage 7.5A after theorem scope survives novelty re-kill and generality/quantifier review.

## Next-stage contract

Stage 6 must compare the **repaired high-gamma theorem**—not the superseded low-gamma version—against the strongest prior-art composite identified at Stage 2. If the only surviving difference is numerical calibration or parameter-domain restriction, return NO-GO. If the repositioning-induced coalition-stability/blocking-threshold effect remains whole-game novel, continue under the narrowed theorem scope.
