# Stage 4R Revival Decisions

Date: 2026-09-10

## Decision 1 — Stage-4A counterexample remains binding

The low-`gamma` crossing SU location equilibrium discovered by Stage 4A is valid and permanent. No repair may suppress it through initialization, branch restriction, no-crossing assumptions, or an unstated equilibrium-selection rule.

## Decision 2 — no new selection/refinement rule

Stage 4R adopts **no** closest-anchor, order-preserving, best-response-dynamics, welfare/Pareto, risk-dominance, or other equilibrium refinement.

The full product circle and all original strategy sets remain unchanged.

## Decision 3 — theorem-domain repair through existing `gamma`

The reopened Stage 4 investigates the existing repositioning-cost curvature `gamma` as the source of the multiplicity boundary. This is a parameter-domain characterization inside the unchanged C1 model, not a new primitive.

Construction search at `gamma=.05` finds no alternative global-BR SU equilibrium on a declared 18-history grid covering multiple `v` and policy depths. This is not promoted to an analytic uniqueness theorem.

## Decision 4 — repaired headline point

Use `(v,gamma)=(.11,.10)` as the conservative Stage-4R construction witness, deliberately away from the observed low-cost multiplicity frontier.

At FULL SU depth `(s_12,s_3)=(.25,.25)`, the construction equilibrium is approximately

`(.14246245,.52420422,.83333333)`.

Approximate welfare:

- IS `.1432822599`;
- SU member `.1433644618`;
- SU outsider `.1419734070`;
- SW `.1425185185`.

## Decision 5 — repaired threshold object

At `gamma=.10`, retain:

- `v_FIX=1/15=.0666666667` exactly;
- `v_EXO-HIST≈.0993400328` as an auxiliary benchmark root;
- `v_FULL≈.1196400688` as the construction FULL root.

The core claim to carry forward is the repositioning-induced B-FIX/FULL blocking-threshold shift after endogenous policy optimization. Do not claim that endogenous policy choice is generically necessary merely from the B-EXO-HIST comparison.

## Decision 6 — Stage 4R verdict

**GO — REPEAT STAGE 4A.**

The unchanged C1 model has a construction-level, non-ad-hoc repair worth independent certification. The words `unique`, `selection-free region`, and the FULL threshold remain uncertified until the repeat Stage 4A passes.

## Decision 7 — next-stage kill condition

Repeat Stage 4A must return to Stage 4 or terminate the C1 revival if it finds an additional relevant SU equilibrium at/around the repaired point that changes welfare, policy, or coalition stability. No selection rule may be added as an automatic fallback.
