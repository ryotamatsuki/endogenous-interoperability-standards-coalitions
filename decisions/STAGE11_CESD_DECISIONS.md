# Stage 11 Decisions — C-ESD

Date: 2026-09-04
Manuscript under review: main SHA `bcad75cdc1e87c54cd2ecfd73559a1e6c96d4c68`
Theory freeze under review: `CESD-THEORY-FREEZE-2026-09-04-v1`

## D-067 — Stage-11 verdict

Decision: **REOPEN EARLIER STAGE — STAGE 4R CONTINUATION-EXISTENCE / POLICY-STAGE REPAIR**.

The project is not killed, but Stage 12 is not authorized.

## D-068 — Canonical on-path witness survives stronger location audit

The canonical FULL SU witness at `(t_bar,v,gamma,s_bar)=(1,0.04,0.11,0.25)` and `(s_12,s_3)=(0.25,0)` passes interval-wise continuous whole-circle unilateral location best responses for all three firms.

Therefore the Stage-11 failure is not an on-path FOC/SOC artifact.

## D-069 — Off-path continuation/SPNE gap is blocking

The Stage-4 policy routine evaluates policy deviations using the fixed-order `loc_nash` continuation without requiring each deviation continuation to pass the whole-circle location-Nash test.

At `(s_12,s_3)=(0.25,0.20)`, the fixed-order continuation has a profitable order-changing location deviation. Stage-11 enumeration of interior stationary candidates across all cyclic orders/anchor branches finds no regular pure location Nash equilibrium at that off-path policy profile.

The current manuscript therefore has not established a pure-strategy SPNE over the stated full standards-depth action set.

## D-070 — Preferred bounded repair

Preferred route: Stage 4R should derive/impose a defensible policy-depth regularity cap under which all feasible policy profiles have valid regular pure location continuations, then re-solve the standards-depth Nash game using only true continuation equilibria.

A preliminary diagnostic at `s_bar=0.20` preserves the headline sign pattern:

- `Delta_M^(B-T)≈-0.008984`;
- `Delta_M^(B-X)≈-0.000434`;
- `Delta_M^(FULL)≈+0.001049`.

A dense 21×21 SU policy-square audit at that cap found no selected-branch whole-circle continuation failure. This is a repair lead, not a new frozen result.

## D-071 — Core novelty survives provisionally

The Ruiz (2004) + Gandal–Shy (2001) synthesis remains a major referee threat, but no audited prior result directly reproduces the FULL-only sign reversal relative to both B-T and B-X. Novelty remains result-level only.

## D-072 — Welfare package survives algebraic audit

The exact national-welfare decomposition and global transfer-cancellation identity remain valid. Witness-specific world-welfare and private/social-position comparisons remain numerical illustrations only.

Reported welfare levels should later be labeled net of the common baseline utility `A`.

## D-073 — Policy-map and institutional claim boundary

The B-T benchmark shows that the baseline policy map alone is insufficient for the reversal. Nevertheless, the within/cross-bloc asymmetry and the binary compatibility graph must remain explicit assumptions. Institutional examples validate policy-controlled interoperability specifications but do not verify strategic re-differentiation or the exact cross-bloc derivative.

## D-074 — Affected-stage routing

Mandatory next action is Stage 4R.

If Stage 4R changes the canonical policy cap/witness or continuation regularity domain, rerun at least:

1. Stage 4 result/verification;
2. Stage 7 welfare/generality;
3. Stage 8 theory freeze;
4. Stage 9 generated-output/reproducibility records;
5. Stage 10 manuscript construction;
6. Stage 11 referee gate.

Stage 6 novelty need not be rerun for a pure parameter-cap repair unless the economic mechanism or claim changes.

## D-075 — Stage 12 blocked

Journal positioning is not authorized until a repeated Stage 11 returns `GO TO JOURNAL POSITIONING`.
