# Stage 4 C-ESD Decisions

Date: 2026-09-04

These decisions follow D-037–D-041 in `decisions/STAGE3R_CESD_POLICY_MAP_DECISIONS.md` and are authoritative together with the canonical decision history until the next consolidation pass.

## D-042 — Freeze weighted-Salop microfoundation

Decision: **ACCEPTED**

Implement the frozen pairwise friction `tau_ij` on the Salop arc connecting adjacent products `i,j`.

For a consumer at distance `y` from `i` on arc `ij`, use

`u_i=A-p_i-tau_ij y+v n_i`.

This yields the exact weighted-Laplacian demand system

`q=b-(1/2)L(Tau)p+(v/2)L(Tau)G_rho q`.

National origin is independent of product taste; each country owns one third of the uniform consumer mass, so `CS_i=CS/3` in the symmetric baseline.

## D-043 — Require whole-circle global location best responses

Decision: **ACCEPTED / STRENGTHENED**

Within a fixed cyclic order the location game is linear-quadratic and the FOCs form a linear system. This conditional stationary point is not sufficient.

Every certified Stage-4 witness must also survive one-firm deviations over the whole circle, including deviations that change cyclic ordering.

Low-`gamma` counterexamples were found in which the SU outsider can profitably jump despite local FOCs/SOCs. Those parameter points are rejected and not repaired.

## D-044 — Verify FULL-only stability reversal

Decision: **VERIFIED**

At

`t_bar=1`, `v=0.04`, `gamma=0.11`, `s_bar=0.25`,

B-T gives

`W_IS=-0.007500 > W_SU_member=-0.017667`,

and B-X gives

`W_IS=-0.014444 > W_SU_member=-0.014878`.

Thus both nested benchmarks uniquely select IS.

In FULL,

`W_SU_member=-0.005929 > W_IS=-0.007500 > W_SU_outsider=-0.046811`,

with `W_SU_member>W_SW=-0.023333`.

The FULL stable set is therefore

`{SU_12,SU_13,SU_23}`,

whereas B-T and B-X have stable set `{IS}`.

## D-045 — Identify the interaction channel

Decision: **VERIFIED**

The headline sign is

`Delta_M=W_SU_member-W_IS`.

At the Stage-4 witness,

`Delta_M^(B-T)=-0.010167`,

`Delta_M^(B-X)=-0.000434`,

`Delta_M^(FULL)=+0.001571`.

Hence the coalition reversal is not generated by policy choice alone or location choice alone. It requires the interaction of endogenous bloc depth with endogenous strategic product repositioning.

## D-046 — Record non-knife-edge region

Decision: **VERIFIED NUMERICALLY**

A 3x3x3 local box around the witness passes the strict FULL-only reversal plus whole-circle SU location best-response check at 23 of 27 points. A wider 5x5x5 audit passes at 108 of 125 points.

The exact witness inequalities are strict, so continuity implies a nonempty open neighborhood on the regular equilibrium branch. Numerical counts document robustness but are not used as a proof substitute.

## D-047 — Stage 4 verdict

Decision: **GO**

C-ESD passes the Minimal Model Gate without changing the Stage-3 frozen policy map.

Route:

`GO -> Stage 6 Novelty Re-Kill`.

Stage 6 must attack the actual proposition:

> Endogenous government standard depth and endogenous firm product repositioning interact to reverse standards-coalition stability: policy-only and location-only benchmarks select IS, while the FULL game makes regional SU stable and IS pair-blockable.

Do not reintroduce relative profit, private interoperability investment, policy costs, endogenous network intensity, transfers, dynamics, lobbying, topology choice or additional countries before the novelty re-kill is completed.
