# Stage 8 Decisions — C-ESD

Date: 2026-09-04
Freeze ID: `CESD-THEORY-FREEZE-2026-09-04-v1`

## D-051 — Theory frozen

Decision: **THEORY FROZEN — GO TO REPRODUCIBILITY SETUP**.

The Stage-7.5-approved C-ESD package is internally consistent with the Stage-4 model, Stage-6 novelty boundary, Stage-7 welfare/generalization record, and existing verification artifacts.

## D-052 — Main contribution frozen narrowly

Only the interaction-induced coalition-stability reversal is authorized as the main theoretical contribution:

`Delta_M^(B-T)<0`,

`Delta_M^(B-X)<0`,

`Delta_M^(FULL)>0`

on a verified regular nonempty region.

No ingredient-level novelty claim may be revived.

## D-053 — Main reversal proof status

The FULL-only reversal is frozen as `CONDITIONAL`, not as an unconditional global analytic theorem.

It is a constructive regular-region result supported by:

- the canonical witness;
- strict payoff inequalities;
- continuity on the selected regular branch;
- whole-circle unilateral location-deviation checks;
- neighborhood parameter audits.

The global lower `gamma_GBR` boundary remains computational rather than closed-form.

## D-054 — Analytic proposition boundary

The following are frozen as `PROVED` on their stated domains:

1. weighted-Laplacian demand characterization;
2. regular interior price equilibrium characterization;
3. fixed-cyclic-order location FOC/SOC system;
4. exact national member welfare decomposition;
5. exact global-welfare transfer-cancellation identity.

Witness-specific welfare rankings and private/social distance comparisons are not global theorems.

## D-055 — Baseline model primitives frozen

The following baseline structure cannot be changed silently:

- three countries/firms;
- formal regimes `IS/SU/SW`;
- bloc-depth policy map;
- fixed network graph conditional on regime;
- unit Salop circle and inherited anchors;
- quadratic redesign cost;
- government-depth -> firm-location -> price timing;
- national welfare `CS/3 + domestic profit`;
- strict-blocking coalition stability.

## D-056 — Essential vs normalization distinction frozen

Essential economic restrictions:

- policy-controlled standard depth;
- within-bloc/cross-bloc SU asymmetry;
- distinct repositionable horizontal product margin;
- costly repositioning with intermediate effective curvature;
- network effects;
- domestic-rent incidence in national welfare;
- government commitment before firm repositioning.

Tractability/normalization devices:

- exact Salop geometry;
- symmetric three-country baseline;
- quadratic cost;
- `CS/3` incidence;
- zero marginal cost;
- full coverage;
- fixed `v` conditional on graph;
- no policy cost;
- cross-bloc coefficient `1/2`.

## D-057 — Closest-paper distinction frozen

The manuscript must defend novelty against the Ruiz (2004) + Gandal–Shy (2001) synthesis attack at the result level.

Authorized distinction: neither close paper contains the full feedback in which endogenous regime-specific standard depth induces endogenous horizontal product repositioning and thereby reverses government coalition ranking relative to both B-T and B-X.

## D-058 — Explicit exclusions

The baseline paper cannot silently add:

- relative-profit objectives;
- private interoperability investment;
- endogenous network intensity;
- direct policy costs;
- transfers/side payments;
- lobbying;
- dynamics;
- endogenous topology;
- more countries;
- heterogeneous national CS incidence;
- alternative spatial geometry;
- empirical estimation.

Such changes require explicit change control and rerunning affected gates.

## D-059 — Next stage

Next canonical stage: **Stage 9 — Repository / Reproducibility Setup**.

Stage 9 must not change the frozen theory. Its task is to make the existing symbolic/numerical/theory package reproducible and production-ready for later Stage 10 section-by-section manuscript construction.
