# Stage 9 Decisions — C-ESD

Date: 2026-09-04
Theory freeze: `CESD-THEORY-FREEZE-2026-09-04-v1`
Starting main SHA: `95755cb82bceb136626279c5ff65fe1f1149afaa`

## D-060 — Frozen verification remains the computational source of truth

Production code must import and serialize the existing Stage-4 and Stage-7 verification modules. It may not reimplement model logic in a second code path.

## D-061 — Generated manuscript numbers are not hand edited

`scripts/generate_outputs.py` generates `tables/generated_results.csv` and `tables/generated_results.tex` directly from the frozen verification modules. Generated files are ignored by Git and regenerated by the build.

## D-062 — Production environment fixed

Python 3.12 is the production major/minor version. `requirements.txt` pins NumPy, SciPy and SymPy. LaTeX compilation uses `latexmk` and a standard TeX Live installation.

## D-063 — One-command gates fixed

- `make verify` runs Stage-4 verification, Stage-7 verification and freeze-consistency checks.
- `make outputs` regenerates manuscript-facing tables.
- `make paper` regenerates outputs and compiles the LaTeX scaffold.
- `make all` runs the complete gate.

## D-064 — CI is the canonical clean-environment gate

GitHub Actions executes dependency installation, frozen verification, deterministic output generation and LaTeX compilation on pull requests and pushes to main. The first Stage-9 CI run completed successfully through all steps.

## D-065 — Stage-10 writing boundary

Stage 10 may write prose and theorem exposition only against the Stage-8 freeze and Stage-9 reproducibility infrastructure. The FULL-only reversal cannot be upgraded from its frozen constructive regular-region proof status. New model primitives require theory change control.

## D-066 — Stage-9 verdict

**REPRODUCIBILITY BASELINE READY.**

Route to Stage 10 — Section-by-Section Paper Construction.
