# Stage 10R Writing Contract

Theory authority: `CESD-THEORY-FREEZE-2026-09-04-v2`.

Stage 10R may refresh prose, notation presentation, theorem exposition, proof presentation, generated tables/figures, and literature discussion so that the manuscript matches the repaired v2 freeze.

Stage 10R may not change players, timing, policy map, utility/demand, costs, network structure, welfare definitions, equilibrium concept, benchmark definitions, proposition truth conditions, proof-status classifications, or coalition-stability logic.

Mandatory repaired-policy wording:

- `s_C` is additional within-coalition harmonization depth conditional on the formal regime;
- a non-singleton bloc chooses `s_C in [0,s_bar]`;
- a singleton bloc has `s_C=0` and no positive harmonization-depth instrument;
- policy-stage claims must rely on verified downstream whole-circle location Nash continuations.

Mandatory dependency order:

1. Model
2. Equilibrium characterization
3. Main results and B-T/B-X/FULL interaction
4. Welfare
5. Approved robustness/generality discussion only
6. Institutional bridge
7. Related literature
8. Introduction
9. Discussion
10. Conclusion

Numerical values appearing in the manuscript must be sourced from `tables/generated_results.*` or directly traceable to the v2 verification chain. Do not hand-edit generated results.

The FULL-only reversal remains a conditional constructive regular-region result. The canonical full-policy-domain continuation validity is computationally verified at the canonical primitives; it is not a global analytic theorem.

World-welfare levels must be described as net of the common baseline utility `A` when numerical levels are reported.

Any desired theory extension triggers v2 theory-change control before manuscript edits proceed.
